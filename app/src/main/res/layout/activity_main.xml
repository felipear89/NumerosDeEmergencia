<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/enable_gps"
        android:id="@+id/btn_getLocation"
    />

    <TextView
        android:id="@+id/txt_currentLocation"
        android:layout_width="wrap_content"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:layout_height="wrap_content" />

    <TextView android:id="@+id/txt_problemMsg"
        android:layout_width="wrap_content"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:layout_toRightOf="@id/btn_getLocation"
        android:layout_height="wrap_content" />
    
    <ListView
        android:id="@+id/listView_emergencyNumbers"
        android:layout_marginTop="@dimen/activity_vertical_margin"
		android:layout_below="@id/btn_getLocation"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />

</RelativeLayout>
